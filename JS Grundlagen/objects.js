let myfancyobject = {
    age : 32, 
    height :173,        // age, heigth und name sind hier keys
    name: "Carlos"
}

//console.log("Objekte " + myfancyobject.age); // nach dem Namen des Objektes wird nach dem Punkt auf den key zugegriffen hier also auf den Wert von
                                            // age im Objekt "myfancyobject" || möglich ist auch der aufruf einer Funktion im Objekt dann mit ()!
